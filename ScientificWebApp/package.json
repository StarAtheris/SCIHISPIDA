{
    "name": "scihispida-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "install-frontend": "cd frontend && npm install",
        "build-frontend": "cd frontend && npm run build",
        "build": "npm run install-frontend && npm run build-frontend && mkdir -p dist && cp -r frontend/dist/* dist/"
    },
    "engines": {
        "node": "18.x"
    }
}